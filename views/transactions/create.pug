form(action='' method="POST")
  select(name="userId")
    each val in user
      option(value= val.id)= val.id
  select(name="bookId")
    each val in book
      option(value= val.id)= val.id
  span &nbsp
  button(type="submit") Add